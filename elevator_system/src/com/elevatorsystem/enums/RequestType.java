package com.elevatorsystem.enums;

public enum RequestType {
    EXTERNAL,   // Hall panel (Up/Down button pressed on a floor)
    INTERNAL    // Inner panel (destination floor selected inside elevator)
}